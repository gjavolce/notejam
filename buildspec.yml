version: 0.2

phases:
  install:
    runtime-versions:
      java: corretto8
  build:
    commands:
    - mvn clean
    - mvn install
artifacts:
  files:
  - 'Dockerfile'
  - 'target/spring-1.0.0-SNAPSHOT.jar'